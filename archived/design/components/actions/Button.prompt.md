Mono operational buttons. One `primary` per screen (accent + glow); `secondary` bordered neutral; `outline` accent-bordered emphasis; `pager` for seq windows; `cta` only on marketing surfaces.

```jsx
<Button variant="primary">＋ New packet</Button>
<Button>clear ✕</Button>
<Button variant="outline">Skim the sample</Button>
<Button variant="pager" disabled>◂</Button>
```
