CLI frame with flat header dots and dark paper. Author lines as children; color with spans.

```jsx
<Terminal title="ci · deploy.yml">
  <div><span style={{color:'var(--text-disabled)'}}>$</span> packets check</div>
  <div><span style={{color:'var(--verified)'}}>✓ forwarded</span> <span style={{color:'var(--text-disabled)'}}>— 5/5 clauses green · 41s</span></div>
  <div><span style={{color:'var(--signal)'}}>⌁ held</span> <span style={{color:'var(--text-disabled)'}}>— handshake below lane floor (strict)</span></div>
</Terminal>
```
